export type {AxisType, FunctionType} from "@entities/";

export type AxesPropsType = {
    x: [string, React.Dispatch<React.SetStateAction<string>>]
    y: [string, React.Dispatch<React.SetStateAction<string>>]
}